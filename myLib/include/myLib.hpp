#ifndef MYLIBRARY_H
#define MYLIBRARY_H

#include <string>
#include <vector>
#include <algorithm>

void sortString(std::string& str);

std::string sumString(std::string& str);

bool analyzeData(std::string& str);

#endif // MYLIBRARY_H
